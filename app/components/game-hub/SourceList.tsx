// Source attribution is intentionally NOT rendered on the page — FinalBoss is
// the source of record. The `sources` arrays are still required in the data and
// enforced by the validator (cite-or-drop / anti-fabrication), so provenance is
// preserved internally; we just don't surface competitor links to readers.
// Flip the return below to re-enable on-page attribution if ever needed.

export default function SourceList(_props: { sources: string[] }): null {
  return null;
}
