// Re-export the canonical EntityDetail view-model from the provider so the
// detail components and the provider share one definition (incl. `blueprint`).
export type { EntityDetail } from '@/app/lib/game-hub/provider';
